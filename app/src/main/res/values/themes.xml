<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Тема окна (launch / system bars). UI рисуется Compose-темой MeloTheme. -->
    <style name="Theme.Melo" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@android:color/background_dark</item>
    </style>
</resources>
